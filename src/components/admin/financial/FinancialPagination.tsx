import React from "react";

interface FinancialPaginationProps { currentRange: string; totalCount: number; page: number; pageSize: number; onPageChange: (page: number) => void; }

const FinancialPagination: React.FC<FinancialPaginationProps> = ({ currentRange, totalCount, page, pageSize, onPageChange }) => { const pageCount = Math.ceil(totalCount / pageSize); return <div className="p-4 border-t border-slate-200 flex items-center justify-between bg-white z-10"><div className="text-sm text-slate-500 font-medium">Exibindo <span className="font-black text-slate-900">{currentRange}</span> de <span className="font-black text-slate-900">{totalCount}</span> resultados</div>{pageCount > 0 && <div className="flex items-center gap-2"><button onClick={() => onPageChange(page - 1)} className="px-3 py-1.5 text-sm border border-slate-200 rounded-lg text-slate-600 disabled:text-slate-400" disabled={page === 0}>Ant</button>{Array.from({ length: pageCount }, (_, index) => index).slice(Math.max(0, page - 2), page + 3).map((pageNumber) => <button key={pageNumber} onClick={() => onPageChange(pageNumber)} className={`px-3 py-1.5 text-sm rounded-lg font-black ${pageNumber === page ? "bg-blue-600 text-white" : "border border-slate-200 text-slate-600 hover:bg-slate-50"}`}>{pageNumber + 1}</button>)}<button onClick={() => onPageChange(page + 1)} className="px-3 py-1.5 text-sm border border-slate-200 rounded-lg text-slate-600 disabled:text-slate-400" disabled={page === pageCount - 1}>Próx</button></div>}</div>; };
export default FinancialPagination;
